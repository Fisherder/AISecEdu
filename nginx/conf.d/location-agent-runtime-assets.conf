# The vendored renderer still has a few root-relative asset references. Keep
# them inside the isolated runtime without forwarding the 玄甲 session.
location ~ ^/(avatars|logos|vendor)/ {
    absolute_redirect off;
    return 308 /agent-runtime$request_uri;
}
